name: CI

on:
  workflow_dispatch:
  push:
    branches: [ master ]

env:
  COMPOSE_FOLDER: GarageSite
  ENV_FILE_NAME: JWTProvider/api.env
  COMPOSE_SERVICE_NAME: jwt-api
  DEV_IMAGE_TAG: dev-${{ github.run_number }}.${{ github.run_id }}.${{ github.run_attempt }}

jobs:
  build:
    uses: DaemonSharps/CI-CD/.github/workflows/build.yml@stable
      
  update_draft_release:
    name: Обновление черновика релиза
    runs-on: ubuntu-latest
    steps:

      - id: release-drafter
        uses: release-drafter/release-drafter@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Set env.RELEASE_TAG
        run: echo RELEASE_TAG=${{ steps.release-drafter.outputs.tag_name }} >> $GITHUB_ENV

  upload_on_hub:
    name: Загрузка образа в hub
    needs:
     - build
     - update_draft_release
    strategy:
      matrix:
        enviroment_name: [ development, production ]
        include:
          - enviroment_name: development
            version_tags: |
              daemonsharps/jwt-auth-api:${{ env.DEV_IMAGE_TAG }}
              daemonsharps/jwt-auth-api:dev-latest
          - enviroment_name: production
            version_tags: |
              daemonsharps/jwt-auth-api:${{ env.RELEASE_TAG }}
              daemonsharps/jwt-auth-api:latest
    uses: DaemonSharps/CI-CD/.github/workflows/docker-deploy.yml@stable
    with:
      enviroment_name: ${{ matrix.enviroment_name }}
      version_tags: ${{ matrix.version_tags }}
    secrets:
      docker_login: ${{ secrets.DOCKER_USER_LOGIN }}
      docker_token: ${{ secrets.DOCKER_USER_AUTH_TOKEN }}

  deploy_on_compose:
    name: Проливка на VPS
    needs:
      - upload_on_hub
      - update_draft_release
    strategy:
      matrix:
        enviroment_name: [ development, production ]
        include:
          - enviroment_name: development
            image_tag: ${{ env.DEV_IMAGE_TAG }}
            image_port: 3000
          - enviroment_name: production
            image_tag: ${{ env.RELEASE_TAG }}
            image_port: 3001
    uses: DaemonSharps/CI-CD/.github/workflows/docker-release.yml@stable
    with:
      compose_folder: ${{ env.COMPOSE_FOLDER }
      env_file_name: ${{ env.ENV_FILE_NAME }}
      compose_service_name: ${{ env.COMPOSE_SERVICE_NAME }}
      image_tag_env: JWT_TAG=${{ matrix.image_tag }}
      image_port_env: JWT_API_PORT=${{ matrix.image_port }}
      environment_name: ${{ matrix.enviroment_name }}
    secrets:
      SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
      SSH_HOST: ${{ secrets.SSH_HOST }}
      SSH_USER: ${{ secrets.SSH_USER }}
      ENV_TEXT: ${{ secrets.ENV_TEXT }}
